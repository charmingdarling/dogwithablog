{{! <div class="blog-container">
  <div class="card-header col-md-10">
    <h2>
      Blog Posts
    </h2>
  </div> }}

<!-- HELLO? THIS IS RIGHT? NOTHING SHOWS UP? -->

<!-- We start using the built-in helper, #each. This helper will iterate over blog post -->
<!-- `blogs` is referencing the const blogs in your router.get("/") of your blog-routes.js at line 24 -->
{{#each blogs as |blogPosts|}}

  <!-- For each blogPost in the array of blogPost objects, the following <div> will be rendered. -->
  <div class="blogPost col-md-5">
    <!-- We use blogPost.id instead of <od> and <li> elements. -->
    <h2 class="blogPost-TitleEntry">
      <a href="/blogPost/{{blogPost.id}}">{{blogPost.title}}</a>
    </h2>
    <p>
      Author:
      {{blogPost.author}}
      <!--Maybe the information eventually goes here, author? time created?-->
    </p>
  </div>
  {{! <div class="col-md-7">
      
    </div> }}
  <!-- Below, we close the #each helper. -->

{{/each}}

{{! </div> }}